{
  "name": "ai-challenge",
  "version": "0.1.0",
  "description": "Claude Code 토큰 사용량 리더보드 CLI",
  "type": "module",
  "bin": {
    "ai-challenge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "files": ["dist", "pricing.json"],
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  },
  "engines": { "node": ">=18" }
}
